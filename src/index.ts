#!/usr/bin/env bun
/**
 * Letta Sync CLI entrypoint
 * 
 * This file serves as the entry point for Bun bundling.
 * It simply imports and runs the CLI module.
 */

// Import and execute CLI - the CLI handles its own argument parsing
import './cli.js';
